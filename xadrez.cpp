/*
first we need to run a test for a 8*8 chest board to know the color of an item.
to do that we need to know the 
if((linha % 2 == 0 && coluna % 2 == 0  )|| (linha % 2 == 1 && coluna % 2 == 1  )){
  1
}else{
    0
}

*/
